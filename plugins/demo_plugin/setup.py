# plugins/demo_plugin/setup.py
from setuptools import setup, find_packages

setup(
    name="demo_plugin",
    version="0.1.0",
    packages=find_packages(),
    include_package_data=True,
    install_requires=[],
)